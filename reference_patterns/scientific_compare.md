Comparison of cataloged ODE simulation against experimental CSV.

Numbers come from `ode_compare` (Lotka-Volterra in P1). The compiler draws
precomputed `observed` and `predicted` series. If PDF text does not match the
closed catalog with explicit parameters, the run stays at `needs_confirmation`.
