# field_evolution

Scalar field over time from a ToolRun array artifact.

Compiler lowering: precomputed RGB stack → `ImageMobject` + `ValueTracker` + named `always_redraw`. The Scene never evaluates a field kernel.
