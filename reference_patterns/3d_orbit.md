# 3d_orbit

Lorenz (and similar) trajectory sets in `ThreeDScene`. Integrator output is a ToolRun; the compiler only samples the array. Ambient camera rotation is optional and has a fixed-camera fallback.
